# T1 Notes

- Expand the README hero badge strip so the package block shows more than version alone.
- Add monthly npm downloads, CI status, and OpenSSF Scorecard badges next to the existing npm, stars, last commit, and license badges.
- Keep the badge row focused on package traction and repo health signals that render well on GitHub.
- Preserve the canonical `about_description.txt` README reference so metadata checks still match the package description source.
